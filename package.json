{
  "name": "raycast2",
  "private": true,
  "scripts": {
    "build": "asc assembly/index.ts --target release",
    "dist": "npm run build:release && mkdir -p dist && cp -r ./build ./dist && cp index.html dist",
    "start": "npx serve . ",
    "types:next": "tsc --noEmit"
  },
  "devDependencies": {
    "assemblyscript": "^0.27.22",
    "typescript": "^5.3.2"
  }
}
